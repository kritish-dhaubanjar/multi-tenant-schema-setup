export default {
  // Indicates whether to automatically clear mock calls and instances before each test
  clearMocks: true,

  // Indicates whether to collect code coverage information during test execution
  collectCoverage: true,

  // Specifies the directory where coverage reports will be generated
  coverageDirectory: 'coverage',

  // Specifies the coverage provider to be used (in this case, V8)
  coverageProvider: 'v8',

  // Specifies the preset to be used for TypeScript Jest configuration
  preset: 'ts-jest',

  // Specifies the test environment to be used (in this case, Node.js)
  testEnvironment: 'node',
}
